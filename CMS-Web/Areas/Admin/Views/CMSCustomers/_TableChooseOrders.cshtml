
@model CMS_DTO.CMSCustomer.CustomerModels


@if (Model.ListOrders != null && Model.ListOrders.Count > 0)
{
    <table class="table table-striped table-hover">
        @*<thead>
                <tr style="background-color:#34495e; color:#fff;">
                    <th style="width:1%;"><input type="checkbox" onchange="ToogleCheckAll(this, '#list-modifiers')"></th>
                    <th>Modifier's Name</th>
                </tr>
            </thead>*@
        <tbody id="list-keywords">
            @for (int i = 0; i < Model.ListOrders.Count; i++)
            {
                <tr data-id="@Model.ListOrders[i].Id" style="cursor:pointer;" class="SpecificItems" onclick ="ShowOrderDetail('@Model.ListOrders[i].Id', '@Model.ListOrders[i].OrderNo')">
                    <td style="width: 60%">
                        <a href="javascript:void(0)">@Model.ListOrders[i].OrderNo</a>
                        @*@Html.CheckBoxFor(model => model.ListOrders[i].Checked, new { @class = "chldItemOngroup", @onClick = "TickCheckBoxForChild(this, " + Model.ID + ")" })*@
                        @Html.HiddenFor(model => model.ListOrders[i].OrderNo, new { @class = "hidName" })
                    </td>
                    <td style="text-align: center"> @Model.ListOrders[i].CreatedDate </td>
                </tr>
            }
        </tbody>
    </table>
}

<script>
    function TickCheckBox(td) {
        var tr = $(td).parents('tr:first');
        $(tr).find('input[type=checkbox]').prop('checked', !$(tr).find('input[type=checkbox]').prop('checked'))
    }
</script>